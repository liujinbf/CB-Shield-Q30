# Kwrt ???????

?????????? `factory.bin` ??? U-Boot ??????????

????:

1. ???????????? `Kwrt/openwrt.ai` Q30 ??
2. ??????????????????? LuCI??? Wi-Fi
3. ???????? `CB-Shield` ?????????????????

## ????

Linux / WSL / Git Bash:

```bash
bash scripts/build-kwrt-runtime-bundle.sh
```

Windows PowerShell:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\kwrt-runtime.ps1 build
```

??????:

- `.work/cbshield-kwrt-runtime.tgz`
- `.work/cbshield-kwrt-install.sh`
- `.work/kwrt-runtime.manifest`
- `.work/cbshield-kwrt-runtime-bundle.tgz`

## ??????

??????????????,???? `/tmp/`:

- `.work/cbshield-kwrt-runtime.tgz`
- `.work/cbshield-kwrt-install.sh`

???? SSH ??:

```sh
chmod +x /tmp/cbshield-kwrt-install.sh
sh /tmp/cbshield-kwrt-install.sh /tmp/cbshield-kwrt-runtime.tgz
```

## ????????

- ??????????
- ???????????,?? `curl`?`jsonfilter`?`uhttpd`
- ???????????? `/etc/cbshield/runtime-backup-???/`
- ?? CB-Shield ??????????
- ??????? defaults
- ?? LuCI ??
- ???? `network`?`wifi`?`rpcd`?`uhttpd`

## ????????

- ??????? `192.168.10.1`
- LuCI ???? `admin/cbshield/dashboard`
- ?? `CB-Shield-Office` ? `CB-Shield-Office-5G`
- `cb-riskcontrol`?`cb-healthcheck`?`cb-ha-monitor`?`cb-dns-guard`?`cb-portal` ???

## ????

1. ?? `http://192.168.10.1/`
2. ?? `opkg list-installed | grep -E 'curl|jsonfilter|uhttpd|luci'`
3. ?? `ps | grep -E 'cb-riskcontrol|uhttpd'`
4. ?? `uci show wireless | grep office_`
5. ?? `/tmp/cbshield-runtime-restart.log`

## ???????

?????:

- ??????
- LuCI ??????
- ????? Portal ??
- ????? Wi-Fi ????

??????:

- ?? DTS / ?? / bootloader ??
- ?????????
- ???????????? `factory.bin`
